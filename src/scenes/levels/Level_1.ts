import { ContainerOptions } from "pixi.js";
import LevelScene from "../LevelScene";

export default class Level_1 extends LevelScene{
    constructor(options?: ContainerOptions)
    {
        super(options);
    }

    
}